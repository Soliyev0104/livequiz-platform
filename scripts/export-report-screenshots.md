# Report screenshots

Placeholder for P12. The final report needs screenshots of:

- Grafana dashboards (latency, throughput, error rate, leaderboard fan-out)
- ClickHouse analytics queries
- Redis sorted-set leaderboard during a live match
- Quiz builder UI
- Live match UI for host + player
- Moderation queue UI
- Compose dependency graph
- ER diagram

P12 will document the exact `docker compose ...` commands and Grafana URLs to capture.
